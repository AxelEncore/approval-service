# Copy to .env to override defaults for local (non-Docker) runs.

# Default is a local SQLite file (zero external dependencies).
# DATABASE_URL=sqlite:///./approval_service.db

# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg2://approval:approval@localhost:5432/approval

LOG_LEVEL=INFO
ENVIRONMENT=local
